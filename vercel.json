{
  "buildCommand": "npx vite build apps/web --outDir apps/web/dist",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
