{
  "buildCommand": "npx vite build apps/web --outDir dist && node -e \"const fs=require('fs'); fs.cpSync('dist', 'apps/web/dist', {recursive: true});\"",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
